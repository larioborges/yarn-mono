{
    "name": "lario",
    "packageManager": "yarn@3.1.1",
    "workspaces": [
        "./packages/*"
    ],
    "devDependencies": {
        "eslint": "^8.5.0",
        "prettier": "^2.5.0"
    },
    "scripts": {
        "lint": "yarn workspaces foreach run lint",
        "test": "yarn workspaces foreach run test"
    }
}

const usersRoutes = require('./users');
const profilesRoutes = require('./profiles');
const pagesRoutes = require('./pages');

module.exports = {
    usersRoutes,
    profilesRoutes,
    pagesRoutes,
};
